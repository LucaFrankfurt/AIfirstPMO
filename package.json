{
  "name": "kolibri",
  "version": "0.1.0",
  "private": true,
  "description": "Kolibri — open source work OS: projects, tasks, cycles, pages. Offline-first, self-hosted, MCP-native.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web",
    "packages/mcp"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:server": "npm run dev -w @kolibri/server",
    "dev:web": "npm run dev -w @kolibri/web",
    "build": "npm run build -w @kolibri/web",
    "start": "npm run start -w @kolibri/server",
    "seed": "npm run seed -w @kolibri/server",
    "typecheck": "tsc --noEmit -p packages/shared && tsc --noEmit -p packages/server && tsc --noEmit -p packages/web && tsc --noEmit -p packages/mcp",
    "test": "node --test packages/server/test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.2"
  }
}
