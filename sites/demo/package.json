{
  "name": "@kolibri/site-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "demo.kolibri.day — the landing page in front of the live demo instance",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node ../csp-seal.mjs dist",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^7.2.6",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
