# Modules

[`architecture.md`](architecture.md) says how Kolibri is built: four packages, one registry, one
write path, no runtime dependencies on the server. That is the *horizontal* story — the layers, and
what each of them is allowed to know.

This file is the *vertical* one. It asks a different question of the same code: **where does one
capability live, and what would it cost to add another one, switch one off, or take one out?** The
answer today is "in ten files, three of which are the largest in the repository", and the rest of
this document is about which ten, why it happened, and what shape fixes it.

It is written to be re-runnable rather than believed. The inventory in the next section is
*generated* by `scripts/modules.mjs` and checked by it in CI, so it cannot quietly stop being true;
the same script enforces the seven rules at the end.

```bash
npm run check:modules      # the rules, and whether the tables below still match
npm run modules            # the inventory on the terminal
npm run modules -- --fix   # rewrite the tables below
```

## Contents

[The shape today](#the-shape-today) · [What already works](#what-already-works) ·
[Where it breaks down](#where-it-breaks-down) · [The target](#the-target-a-kernel-capabilities-and-adapters) ·
[The module contract](#the-module-contract) · [Getting there](#getting-there) ·
[The rules, and who enforces them](#the-rules-and-who-enforces-them)

## The shape today

<!-- generated by scripts/modules.mjs — run `npm run modules -- --fix` -->

40 modules, 269 source files, 76 795 lines, in four rings.

| Ring | Lines | Share | What it is |
|---|---:|---:|---|
| **kernel** | 21 895 | 29% | Always present. Nothing works without it and nothing switches it off. |
| **capability** | 35 731 | 47% | The things the product does. Ordered rather than independent — see the table below. |
| **adapter** | 17 295 | 23% | Edges facing something outside the process: a protocol, a provider, a file format. |
| **shell** | 1 874 | 2% | Composition only — what starts, in what order, wired to what. |

### Kernel

| Module | Files | Lines | Largest file |
|---|---:|---:|---|
| `i18n` | 5 | 7 800 | `kernel/i18n/locales/en.ts` (2 452) |
| `registry` | 5 | 2 476 | `kernel/registry/types.ts` (1 656) |
| `identity` | 10 | 2 172 | `kernel/identity/routes/auth.ts` (521) |
| `write-path` | 3 | 1 920 | `kernel/write-path/repo.ts` (858) |
| `design-system` | 23 | 1 832 | `kernel/design-system/ui.tsx` (525) |
| `platform` | 11 | 1 775 | `kernel/platform/env.ts` (537) |
| `sync` | 7 | 1 763 | `kernel/sync/routes/sync.ts` (467) |
| `search` | 5 | 1 158 | `kernel/search/routes/search.tsx` (586) |
| `files` | 6 | 700 | `kernel/files/zip.ts` (315) |
| `mail` | 3 | 299 | `kernel/mail/mailbox.ts` (129) |

### Capabilities

| Module | Files | Lines | Switch | Largest file |
|---|---:|---:|---|---|
| `work` | 25 | 6 119 | — | `modules/work/views.tsx` (996) |
| `planning` | 11 | 4 260 | — | `modules/planning/routes/projects.tsx` (1 491) |
| `pages` | 13 | 3 741 | — | `modules/pages/Markdown.tsx` (672) |
| `budgets` | 4 | 3 239 | `budget` | `modules/budgets/routes/budgets.tsx` (1 669) |
| `mail` | 15 | 3 109 | `mail` | `modules/mail/settings.tsx` (523) |
| `operations` | 9 | 3 065 | — | `modules/operations/routes/settings.tsx` (998) |
| `chat` | 6 | 2 483 | — | `modules/chat/routes/chat.tsx` (1 428) |
| `kpis` | 4 | 1 842 | `kpi` | `modules/kpis/routes/kpis.tsx` (935) |
| `infrastructure` | 4 | 1 627 | `infrastructure` | `modules/infrastructure/landscape.tsx` (568) |
| `guide` | 6 | 1 625 | — | `modules/guide/diagrams.tsx` (628) |
| `time` | 6 | 1 490 | `time` | `modules/time/rates.tsx` (581) |
| `automation` | 3 | 1 485 | — | `modules/automation/settings.tsx` (729) |
| `ai-review` | 4 | 535 | `ai` | `modules/ai-review/task-review.tsx` (204) |
| `trash` | 2 | 433 | — | `modules/trash/trash.tsx` (217) |
| `notifications` | 2 | 296 | — | `modules/notifications/effects.ts` (219) |
| `intake` | 1 | 227 | — | `modules/intake/intake.tsx` (227) |
| `share` | 1 | 155 | — | `modules/share/share.tsx` (155) |

### Adapters

| Module | Files | Lines | Largest file |
|---|---:|---:|---|
| `mcp` | 15 | 6 216 | `adapters/mcp/kit.ts` (1 151) |
| `transfer` | 8 | 3 337 | `adapters/transfer/transfer.ts` (793) |
| `webhooks` | 6 | 1 920 | `adapters/webhooks/foreign.ts` (747) |
| `imap` | 4 | 1 187 | `adapters/imap/imap.ts` (364) |
| `mail` | 6 | 1 114 | `adapters/mail/mail.ts` (426) |
| `oauth` | 4 | 950 | `adapters/oauth/routes/oauth.ts` (497) |
| `telegram` | 5 | 756 | `adapters/telegram/telegram.ts` (460) |
| `share` | 1 | 479 | `adapters/share/routes/share.ts` (479) |
| `push` | 4 | 383 | `adapters/push/push.ts` (209) |
| `ai` | 5 | 344 | `adapters/ai/ai-gemini.ts` (127) |
| `calendar` | 2 | 340 | `adapters/calendar/routes/calendar.ts` (195) |
| `s3` | 2 | 269 | `adapters/s3/s3.ts` (227) |

### How the capabilities lean on each other

| Capability | Leans on | Leaned on by |
|---|---|---:|
| `pages` | — | 3 (`ai-review`, `automation`, `work`) |
| `share` | — | 2 (`intake`, `work`) |
| `work` | `ai-review`, `pages`, `planning`, `share`, `time` | 2 (`intake`, `kpis`) |
| `ai-review` | `pages` | 1 (`work`) |
| `budgets` | — | 1 (`kpis`) |
| `guide` | — | 1 (`automation`) |
| `notifications` | — | 1 (`automation`) |
| `operations` | — | 1 (`automation`) |
| `planning` | — | 1 (`work`) |
| `time` | — | 1 (`work`) |
| `trash` | — | 1 (`automation`) |
| `automation` | `guide`, `notifications`, `operations`, `pages`, `trash` | — |
| `chat` | — | — |
| `infrastructure` | — | — |
| `intake` | `share`, `work` | — |
| `kpis` | `budgets`, `work` | — |
| `mail` | — | — |

### What each ring asks the next one for

| Asked by | For | Filled by |
|---|---|---|
| `kernel/design-system` | the link into the guide that `Empty` offers <sub>`provideGuideHint`</sub> | `capability/guide` |
| `kernel/files` | a place to put bytes <sub>`registerBackend`</sub> | `adapter/s3` |
| `kernel/search` | a corpus with an index and a visibility rule of its own <sub>`registerCorpus`</sub> | `capability/mail` |
| `kernel/sync` | a handler for one kind of frame on the live connection <sub>`onStream`</sub> | `capability/chat` |
| `kernel/write-path` | a listener run once the write has committed <sub>`onCommitted`</sub> | `adapter/webhooks`, `capability/notifications` |
| `kernel/write-path` | a listener run inside the write's own transaction <sub>`onWrite`</sub> | `capability/automation` |
| `capability/ai-review` | a model that can answer, under a configuration name <sub>`provideModel`</sub> | `adapter/ai` |
| `capability/automation` | a chore for the periodic sweep to run <sub>`onSweep`</sub> | `adapter/telegram`, `adapter/webhooks` |
| `capability/mail` | a provider that can mint a mailbox token <sub>`registerMailAuthProvider`</sub> | `adapter/oauth` |
| `capability/mail` | a transport that can read a mailbox <sub>`registerMailFetcher`</sub> | `adapter/imap` |
| `capability/notifications` | a channel a notification is carried on <sub>`onNotification`</sub> | `adapter/push`, `adapter/telegram` |

<!-- end generated -->

Two rows in those tables were the whole diagnosis. `notifications` was **58 lines** — writing a
notification is one small function, because everything it *meant* had been distributed into
`repo.ts` and everything it *did* into five adapters. It is **296** now, in two files, and the
growth is the fix: the meaning came home when `repo` started offering `onCommitted`, and the
delivery stayed in the adapters but through a port they register with rather than a name the
capability calls. And `adapter/mcp` is still the largest thing here: the assistant's view of the
product is larger than the product's own largest capability, because every tool re-derives from SQL
what a screen already knows. It is fifteen files rather than three now, which makes it readable; it
does not make it smaller, and finding 7 is why.

## What already works

Four seams in this codebase are load-bearing and should be extended rather than replaced. A
reorganisation that breaks any of them is a worse architecture with a tidier directory listing.

**The entity registry.** `shared/src/kernel/registry/entities.ts` — 43 entities, six flags, one line per field —
generates the server's write path, the REST routes, the sync queries, the client's IndexedDB stores
and the merge rules. Adding a field is one line plus a column. This is the single best thing in the
repository and every proposal below is a way of making *more* of the system derive from it, not less.

**Pure domain logic in `shared`.** `budget.ts`, `kpi.ts`, `rates.ts`, `landscape.ts`,
`schedule.ts` — 2 293 lines of arithmetic and rules with no I/O, imported by the server, the web app
and the MCP tools alike. Money is rounded the same way in a screen, in a report and in an export
because there is one function. `shared` imports nothing from `server` or `web`, and the check
enforces it.

**Two real provider interfaces.** `adapters/ai/ai.ts` has three model providers behind one
`Reviewer` type; `kernel/files/storage.ts` has disk and S3 behind one interface with the same
content-addressed key. Both are
small, both are honest about what varies (`ai.ts`: "the URL, the header the key goes in, where the
prompt sits, where the answer sits"), and both are the template for the adapter ring.

**One write path.** Every mutation — REST, sync push, MCP tool, seed script, automation rule — goes
through `writeEntity`. That is why merge semantics, the activity trail, notifications and the search
index cannot drift apart between entry points. The problem below is not that this exists; it is that
`writeEntity` was made to *contain* what it should *dispatch to*.

## Where it breaks down

### One capability, traced

Budgets is the newest optional feature and the cleanest specimen: it is switched off by default, it
has its own tables, and nothing else in the product needs it. Here is everywhere the word appears
outside its own three files.

| File | Mentions | What lives there |
|---|---:|---|
| `web/locales/{en,de,fr}.ts` | 191 each | Every string, in three catalogues |
| `server/lib/mcp/` | 141 | Seven tools in `tools/budgets.ts` (87), the rest scattered through the shared kit and four other groups |
| `server/lib/repo.ts` | 73 | Invariants, cascades, `canSeeBudget` |
| `shared/types.ts` | 43 | Four interfaces, the constants, the switch |
| `server/db/schema.sql` | 25 | Four tables |
| `shared/entities.ts` | 21 | Four registry entries |
| `server/routes/entities.ts` | 16 | Collection names and guards |
| `web/routes/settings.tsx` | 5 | The switch that turns it on |
| `web/App.tsx` | 7 | Two routes, now fetched rather than imported |
| `web/lib/nav.ts` | 2 | One destination |

Ten files, three of them the largest in the repository, none of them about budgets. That is the
whole problem stated once: the capability is real, its boundary is not.

Splitting the MCP file (finding 1) moved 87 of those 141 mentions into a file that *is* about
budgets — which is the shape the rest of this document is after, and also the measure of how far
there is to go: the other 54 are still spread across a shared kit and four unrelated tool groups.

### The findings

> Paths in this section are the ones that existed when each finding was written. Step 10 moved every
> file into `<ring>/<module>/`; the inventory above is where things are now.
>
> **The numbers are records, not measurements.** A bold figure in this document is one or the other,
> and `figures.mjs` will not let it be neither: a *measurement* has a claim there and fails the build
> when the tree disagrees, a *record* has a `HISTORY` entry saying what it is a record of. Everything
> below is a record — `2 370 lines`, `108 crossings`, `1 030 selector runs` — and must not be
> "corrected" to what the tree says today, because then the argument it is evidence for loses its
> evidence. `repo.ts` came down to 1 306 lines at step 7b; it is 858 now; both are true and only the
> second is checked.

Seven of them, each with the evidence and each with a fix, ordered by what they cost.

### 1. `lib/mcp.ts` was a second application — **fixed**

5 464 lines, five exports, 72 tools and 5 prompts in one `TOOLS` array, with **126 raw SQL
statements** inline. By tool group:

| Group | Tools | Lines |
|---|---:|---:|
| Reports | 8 | 729 |
| Planning | 12 | 592 |
| Tasks | 9 | 539 |
| Budgets | 7 | 461 |
| Configuration | 6 | 366 |
| KPIs | 5 | 361 |
| Infrastructure | 6 | 340 |
| Rates | 4 | 229 |
| Pages | 6 | 197 |
| Workspace | 6 | 185 |
| Attachments | 3 | 184 |

Adding a capability meant editing this file. Removing one meant editing this file. The eleven groups
map one-to-one onto capability modules that already exist — it was a directory listing written as an
array.

It is a directory now:

| | Lines |
|---|---:|
| `mcp/index.ts` — the envelope: protocol version, prompts, dispatch, and the eleven-line assembly | 265 |
| `mcp/kit.ts` — the contract (`ToolDef`, `McpCtx`, `McpError`) and the vocabulary all eleven share | 1 149 |
| `mcp/tools/reports.ts` … `mcp/tools/pages.ts` — one file per group | 190–740 |

The helpers are what made the split work: they were the thing the groups shared, so pulling them out
left each group as a plain array of tool definitions that reads like a list of what an assistant can
do — which is what it is. Two other things fell out of it. `prepare_meeting` reached into `TOOLS` to
call the other six reports by name and now asks `reportTools`, the array it is a member of, which is
a shorter reach and a truer one. And `figures.mjs` used to tell tools from prompts by which list a
`name:` sat in, because they shared a file; they are separate files now, so it counts each where it
lives.

Nothing about the surface moved: 72 tools, the same names, no duplicates and none missing — checked
by calling `toolNames()` against the list from before, and again through the browser, where the
walkthrough asserts a freshly authorised token reaches 72 tools. Only the order changed, and only
across groups: `search` used to sit between `list_time` and `list_templates`. Nothing depends on it —
`tools/list` is a set to a client, and the two tests that read it ask by name.

It was also where both of the repository's layering violations lived: `lib/mcp.ts` imported
`storeFile` from `routes/files.ts` and `searchWorkspace` from `routes/search.ts`. **Fixed** — both
moved down into `lib/uploads.ts` and `lib/search.ts`, where the route and the MCP tool are equal
callers of one implementation rather than one of them reaching up into the other. There are now no
layering exceptions at all, which is worth more than two: nothing is left to cite as precedent.

> **Remaining.** The **12** files sit under the adapter rather than beside the capability whose
> product they expose, and the module map says so: they are all `adapter/mcp`. This note used to say
> that moving `tools/budgets.ts` next to `budgets` was step 10's job. Step 10 came and went — it put
> every file under `<ring>/<module>/` and left these where they were — so the job is nobody's until
> somebody takes it, and it still needs the one thing it always did: `ToolDef` is the contract a
> capability and the envelope agree on, so it wants a home a capability may import without importing
> an adapter. A workspace with budgets off also still *loads* those tools
> and checks the switch per call; assembling the list per workspace instead is a behaviour change and
> was deliberately left out of a refactor.

### 2. `lib/repo.ts` contained the rules it should dispatch to — **fixed**

2 370 lines. The generic part — merge, tombstones, references, the transaction — is about 400 of
them. The other 1 900 were domain-specific and belong to capabilities that already exist elsewhere:

| In `repo.ts` | Belongs to |
|---|---|
| `applyBudgetInvariants`, `tombstoneBudgetChildren`, `detachActualsOf`, `canSeeBudget` | `budgets` |
| `applyKpiInvariants`, `tombstoneKpiChildren`, `detachTargetsOf`, `canSeeKpi` | `kpis` |
| `applyLandscapeInvariants`, `detachFromVendor`, `detachComponent` | `infrastructure` |
| `applyRateInvariants` | `time` |
| `applyChannelInvariants`, `applyMessageInvariants`, `guardChannelWrite`, `guardMessageWrite`, `guardReadStateWrite`, `reconcileReactions`, `canSeeChannel`, `notifyLevel` | `chat` |
| `applyPageInvariants`, `snapshotPage`, `mergeCrdt` | `pages` |
| `applyTaskInvariants`, `guardTransition`, `cascadeSchedule` | `work`, `planning` |
| `notify` (172 lines), `fireWebhooks` (172), `taskPayload`, `recordActivity` | `notifications`, `adapter/webhooks` |

`applyInvariants` alone was a 194-line switch over entity names. Every new capability made it
longer, and every capability that could be removed could not be, because its rules were interleaved
with everyone else's inside one function.

**`writeEntity` has the hook points now.** `repo.onEntity` takes a rule that names the entities it
speaks for and fills in whichever of three moments it cares about — `defaults` on create,
`invariants` on every write, `effects` once the row is written. Registration is in `src/wiring.ts`,
beside the write listeners from step 4, and it is the same idea one floor up: synchronous, in order,
inside the transaction.

The argument that makes appending safe is worth writing down, because it is the whole reason this
could be done mechanically rather than by re-deriving each rule: **one write is one entity.** Two
rules for different entities never both run, so their relative order cannot matter. What has to be
preserved is the order *within* an entity — and that is the order they are registered in, which is
the order they were branches in.

All eight domains have moved — `rules/work.ts`, `rules/pages.ts`, `rules/chat.ts`,
`rules/planning.ts`, `rules/budgets.ts`, `rules/kpis.ts`, `rules/infrastructure.ts`,
`rules/rates.ts` — and each file sits in its own capability in the table above rather than in the
kernel. `repo.ts` came down to **1 306 lines** from 2 370 at that step, and to **858** later, when
`notify` and `fireWebhooks` moved out of the write path behind `onCommitted`.

| | Was | Now |
|---|---:|---:|
| `applyInvariants` | 194 | gone — a one-line dispatch at its call site |
| `applyCreateDefaults` | 120 | 36 |
| `afterWrite` | 81 | 47 |

That the rules are load-bearing rather than decorative was checked by not registering them: 29 tests
fail for the four switchable capabilities, 190 for the four core ones.

The `guards` hook came with the core domains and is the one that needed care. Chat's three checks and
`guardTransition` are **permission** checks — who may write into a private conversation, whose
message this is to edit, which roles may receive work — and each was an `if (entity === … &&
!opts.system)` branch. The `!opts.system` half belongs to the hook now rather than to each rule,
because a check a module has to remember to gate is a check that will be forgotten. The limits below
say modules are not a permission boundary; this does not make one optional, because a rule that is
not registered is not there at all, which `wiring.ts` and its test exist to prevent.

Two of the moved rules failed to announce themselves, and that is the more useful finding. A budget
line follows its budget's workspace and a KPI reading follows its KPI's — and *no test could tell
either apart from doing nothing*, because `guardReferences` refuses a cross-workspace parent one
step earlier. They are not doing nothing: a `system` write skips that guard, and imports, restores
and transfers all take it. `entity-rules.test.ts` drives them from there, and was itself checked by
disabling each rule.

One thing a mechanical move damages quietly, and it is worth naming because the first attempt did it:
**the reasoning**. Three `//` comments were left standing over code that had gone, because the
extractor only walked back over `/** */` blocks. Nothing was lost — the check is to compare every
comment line in the old file against the new set and require the difference to be empty — but three
had to be carried to where their code went.

> **Remaining.** **16** `if (entity === …)` branches are left — in `notify`, `fireWebhooks` and
> `applyCreateDefaults`, which are `modules/notifications/effects.ts`, `adapters/webhooks/effects.ts`
> and `repo.ts` since the write path handed the first two out. They are a different thing from a
> domain rule: the same three effects for
> every entity, with per-entity field mapping — which field holds prose a mention can be written
> into, what a row is called in a notification, which anchor it hangs off. That is a descriptor per
> entity, and the entity registry is where a descriptor belongs. (An earlier draft of this document
> pointed at finding 7's `queries.ts` here. It was wrong: none of the sixteen is a read.)

### 3. The write path and the rules engine hold each other up

`repo.ts` → `automation.ts` → `repo.ts`, and `repo.ts` → `automation.ts` → `bootstrap.ts` →
`repo.ts`. Three files that can all reach each other, so none of them can be understood, tested or
replaced alone.

The relationship is real — the write path calls the rules engine and the rules engine writes rows.
Only the *import* was accidental, and being precise about which part mattered, because the obvious
fix was wrong.

`afterWrite` said so itself: *"`runAutomations` stays inline on purpose: what it writes must land
inside the transaction, and what those writes notify comes back through here and queues itself."*
Publishing on `lib/bus.ts` and having `automation` subscribe would have made that call asynchronous,
moving a rule's writes outside the transaction and breaking `create_tasks_batch`'s "every task or
none" for exactly the rows a rule generated — the failure `withEffectsHeld` exists to prevent, let
back in through the other door.

**Fixed by dependency inversion, not asynchrony.** `repo` exposes `onWrite`; `automation` registers
`runAutomations` for it. The call is still an ordinary function call, in the same place, synchronous,
inside the transaction — an array of listeners rather than a bus, deliberately. Only the import
direction moved. `withEffectsHeld` and the external-effects queue are untouched.

It bought one new way to go wrong that the old shape did not have: a binary that writes rows without
wiring the listeners up runs no rules at all, silently. So `src/wiring.ts` is the single list of what
hangs off the write path, both entry points call it, and `wiring.test.ts` checks the list against the
files *and* against the scripts in `package.json`, so a third entry point cannot appear unaccounted
for. That the wiring is load-bearing rather than incidental was checked the only way worth checking:
removing it fails eight of the thirteen automation tests.

> **What this sets up.** `onWrite` is the general case of the `effects` hook in the module contract
> below. Step 7 does not need a new mechanism — it needs more registrations against this one.

### 4. Three capabilities on the client hold each other up

`budget.tsx` → `insights.tsx` → `rates.tsx` → `budget.tsx`. Budgets borrowed a table from planning,
planning borrowed "may this person see money" from time, and time borrowed a money input back from
budgets. A second knot, `views.tsx` ↔ `saved-views.tsx`, was a view's *shape* living in the screen
that renders it. Underneath both, `components/` holds 53 files from every capability side by side,
so which screen a component belongs to is a guess from its name.

Both knots are **fixed**, and none of it needed the callers rearranged — only the shared thing moved
down to where all of them could reach it: `asMoney` and `MoneyInput` into `components/ui/money.tsx`,
the chart table into `components/ui/table.tsx`, `useSeesMoney` into `session.tsx` (it reads a role
and nothing else), and `ViewConfig` with `DEFAULT_VIEW` into `task-parts.tsx`, which both files
already imported `GroupBy` from.

What is left is the directory: 53 components in one folder is still 53 components in one folder.

> **Closed by step 10, though not the way this asked.** It said to move `components/` into
> `modules/<name>/`. What happened instead is that the folder became `kernel/design-system` — the
> parts every screen is built from are kernel, not a capability's — and the cross-capability imports
> it wanted made visible are visible: a project page hosting the KPI and budget panels is a row in
> [the capability table](#how-the-capabilities-lean-on-each-other), and rule 6 keeps the whole of it
> acyclic. Neither half of the note is left.

### 5. Nothing was code-split, so every workspace downloaded every feature — **fixed**

`npm run build` produced one **1 304 kB** chunk (385 kB gzipped) plus a lazily-loaded mermaid. Vite
warned about it. In that chunk, for a workspace with the optional features off, sat the budget
screens, the KPI screens, the register, the timesheet, the manual, and two locale catalogues nobody
had selected — roughly a sixth of the source tree, parsed and evaluated on a phone that would never
render a word of it. The switch already existed and `lib/nav.ts` already read it: a workspace without
budgets got no link to budgets and all of the code anyway.

| | Chunk | Gzipped |
|---|---:|---:|
| Before | 1 304 kB | 385 kB |
| After | **819 kB** | **253 kB** |
| | −37% | −34% |

Split out: `de` (138 kB) and `fr` (142 kB), `settings` (79), `budgets` (36), `infrastructure` (30),
`kpis` (27), `help` (13), `rates` (13), `timesheet` (1).

Three things this needed that "add `React.lazy`" does not say:

- **English stays static.** It is the fallback `translate` reaches for on a missing key *and* the
  catalogue the key type is derived from; neither survives being fetched. `main.tsx` awaits the
  detected locale before the first render, so a German reader gets German rather than a frame of
  English — the language equivalent of the theme flash the line above it prevents.
- **A capability's panels count as the capability.** `routes/projects.tsx` imported `ProjectKpis`
  and `MilestoneKpis` statically, which pulled all 1 676 lines of the KPI screen straight back into
  the first load and quietly cancelled the split. Both are fetched now, behind the switch at the
  call site rather than inside the component, because a check that runs after the import is too late
  to stop one. `ProjectBudget` was the same.
- **Splitting would have cost offline.** The service worker caches `/assets/` on first *request*, so
  a chunk nobody has opened is a chunk that is not on the device — and opening Settings on a train
  would have found nothing. The screens are warmed in `requestIdleCallback` after the app is up,
  narrowed by the same switches the navigation uses. Off the critical path, on the disk a second
  later. And because a chunk can still be missing — a deploy replaces the file whose fingerprinted
  name an open tab remembers — the route switch sits inside an error boundary that says so and
  offers a reload, rather than failing as a blank area and an uncaught rejection.

> **Remaining.** Chat, the board and the task sheet stay eager on purpose: a screen somebody reaches
> several times an hour must not spend a frame on a spinner to save bytes on a file they were always
> going to need. The next honest win there is not splitting but mermaid, which is already lazy and
> is by far the largest thing on disk — it does not arrive alone, but with cytoscape and a renderer
> per diagram kind, and that family outweighs everything else lazy, katex included.

### 6. The client store recomputed everything on every change — **fixed**

`lib/store.ts` kept one global `version`. Any write to any entity bumped it, every
`useSyncExternalStore` listener fired, and all **193** `useQuery` call sites recomputed. A chat
message typed in another channel invalidated the budget dashboard's selectors — and, worse than the
scan itself, handed every caller a freshly allocated array, so each `useMemo` and `React.memo`
keyed on one of those arrays fell over too.

Measured on a board with a task sheet open, ten writes to one task row: **1 030 selector runs**, or
103 per keystroke.

**Fixed with a counter per table, and a read set nobody has to declare.** The counters were the easy
half. The hard half was knowing which tables a selector depends on, and the shape the original fix
proposed — `useQuery(fn, deps, ['task', 'state'])` — is the shape that fails: a caller lists the
tables it thinks it reads, someone adds a `byId('label', …)` three calls deep inside a helper, and
the screen quietly stops updating. There is no compiler for that and no test that would notice.

So the read set is *observed* instead. `list` and `byId` are the only ways into the cache, so each
records the table it touched into whatever selector is running; the memo keys on the sum of those
tables' counters. A selector that reads `task` and `state` cannot forget to say so, and a selector
that starts reading a third table on its second run is right from that run onward. The same ten
writes now cost **100 selector runs**, ten per keystroke — the ones that actually read `task`.

This closed the last read that went around `list` and `byId`: `trash.tsx` walked `tables` directly,
because tombstones are the one thing `list` hides. A read the memo cannot see is a selector that
would have kept its first answer for ever, so the store offers `listAll` and the screen calls it.

> **Remaining.** The component still re-renders on every write; only the recomputation stopped.
> **89** reads — counted when step 9 shipped, by reading the call sites rather than by a rule a
> script could re-apply, which is why this is a record rather than a figure — go straight from a
> render body or a `useMemo` into the cache rather than through `useQuery`,
> and a global re-render is the only thing telling them a row moved. Narrowing the subscription
> before those move behind hooks would not be an optimisation, it would be a staleness bug in 89
> places — so the cheap half shipped and the rest waits for them. A re-render that recomputes
> nothing and returns identical children is most of the way there in any case.

### 7. Reads had no home, so the assistant and the screen disagreed — **the definitions are fixed**

631 SQL statements across the server; no query layer. The consequence named here was not duplication
of code but duplication of *definitions*: `deadlines_at_risk` called work at risk if it was overdue,
blocked, unstarted or unassigned; the interface painted a due date red if `due < today`, and looked
at nothing else. Both defensible. Neither aware of the other. A person who asked the assistant and
then looked at the screen got two answers to what they thought was one question.

The screen's answer was also wrong, and had been from the start: a bare date meant a task somebody
shipped in January stayed red for ever. Nobody had noticed, because the class it set had no effect
either — `.due-overdue` was a hand-written rule, the chip it lands on already carries `text-soft`
from `chipVariants`, and a Tailwind utility beats a plain rule. Two faults cancelling out into no
colour at all, which is why a wrong definition could sit there unread.

**Fixed by a shared predicate.** `shared/src/modules/work/risk.ts` states the rule once: `riskOf` for the report,
`dueTone` for the chip, `isDoneGroup` for anything asking whether work has stopped. It is pure, and
the caller passes in what today is — a server in one timezone and a phone in another do not agree
about that either, and only the caller knows which day it meant. Eighteen places across twelve files
had spelled `group === 'completed' || group === 'cancelled'` out by hand — nine screens, three
server files, and `deadlines_at_risk`'s own arithmetic. All of them call it now.

**The `queries.ts` half was dropped, and the measurement is why.** The premise was that reads with
no home duplicate *definitions*. Counting them: 359 distinct `SELECT`s in the server, 21 of them
written in more than one file — six per cent — and every one of the 21 is a keyed lookup or an
existence check. "The task with this id." "The workspace's name." "Is this person a member." Not one
is a definition of anything. The single real one was "at risk", and it is now in `shared`, where a
`queries.ts` could not have put it, because the client cannot call server SQL.

The rest of the premise was answered by steps 6 and 7 without being aimed at: each MCP group's reads
now sit in its own tool file and each capability's write rules in its own rules file, so the SQL
already has a home per group. Moving 631 statements into per-capability `queries.ts` files to
de-duplicate 21 one-line lookups is a large diff that buys tidiness and risks behaviour. It is not
worth it, and saying so is cheaper than doing it and finding out.

**The other half of the finding was real, and is done.** `routes/auth.ts` was 957 lines and 41
endpoints covering sign-in, OIDC, 2FA, sessions, the account, API tokens, workspaces, members,
invites, unsubscribe links, mail status, bounce handling, Telegram linking and push registration —
eight concerns in one file, four of them belonging to adapters that already existed. The module map
called all 957 lines `identity` and could not see that a quarter of them were not, because it works
one file at a time.

It is five files now: `auth.ts` (17 endpoints, 498 lines) for signing in and the account,
`workspaces.ts` (10) for workspaces, members and invites, and `mail.ts` (7), `telegram.ts` (4) and
`push.ts` (3) beside the adapters they belong to. `sessionInfo` moved down to `lib/auth.ts`, because
two route files answer with it. No endpoint changed, and all 41 were checked live as well as by the
suite.

The split has one failure mode a single file did not: a `register…Routes` nothing calls compiles,
typechecks, imports cleanly and serves 404 for everything in it. `routes.test.ts` checks the
registrations against the files — comments stripped, because the way this actually breaks is a call
somebody commented out. Counter-proof: commenting out one registration fails it, and the typechecker
says nothing.

## The target: a kernel, capabilities and adapters

```
                         ┌──────────── adapters ────────────┐
                         │  mcp   rest   webhooks  calendar │
                         │  mail  push   telegram  share    │
                         │  s3    ai     oauth     transfer │
                         └───────────────┬──────────────────┘
                                         │  a protocol, a provider, a format
                         ┌───────────────▼──────────────────┐
                         │          capabilities            │
                         │  work  planning  pages  chat     │
                         │  time  budgets   kpis   infra    │
                         │  automation  ai-review  guide    │
                         └───────────────┬──────────────────┘
                                         │  entities, invariants, tools, queries, screens
                         ┌───────────────▼──────────────────┐
                         │             kernel               │
                         │  registry · write path · sync    │
                         │  identity · files · search       │
                         │  i18n · platform · design system │
                         └──────────────────────────────────┘
```

Three rules, and they are the whole architecture:

1. **Capabilities depend on the kernel, never on each other.** Where two genuinely need the same
   thing — a money input, a visibility hook, a table — that thing is kernel, not a shortcut between
   them. Finding 4 is what the rule is for.
2. **Adapters depend on capabilities, never the other way round.** An adapter is a translation of
   something the product already does into a protocol somebody outside speaks. If a capability needs
   an adapter, the capability is holding logic that belongs to it — `notifications` at 58 lines is
   that mistake seen from the other side.
3. **The kernel depends on nothing above it.** It is what remains when every capability is removed,
   and it must still start, sync, authenticate and serve an empty workspace.

What this buys, concretely: a capability can be added by adding a directory; switched off without
being loaded, on the server *and* in the browser; tested with the kernel and nothing else; and
removed by deleting a directory and one line of registration. None of those is true today for any of
the fifteen.

### Which ring a thing belongs in

The test is a question about failure, not about size. **If this were deleted, what breaks?**

- Everything, immediately, including an empty workspace → **kernel**.
- One area of the product, and whatever leans on it → **capability**.
- One way of reaching the product; the product itself is unchanged → **adapter**.

`search` is kernel because the index is written in the same transaction as the row, so removing it
would change the write path. `trash` is a capability because tombstones would still exist, they would
just have no screen. `mcp` is an adapter because everything it does, a person can already do.

The second answer used to read "the rest keeps working", which was the flat reading and is not what
the graph says: delete `pages` and three capabilities stop compiling. Capabilities are a sparse
partial order, not a set of peers — see [the table](#how-the-capabilities-lean-on-each-other) and the
argument against making a ring of it.

## The module contract

A capability is a directory on each side. Since step 10 that is literally true — this is what
`budgets` is today, across all three packages:

```
packages/shared/src/modules/budgets/
  budget.ts       the arithmetic both sides do

packages/server/src/modules/budgets/
  rules/budgets.ts   defaults, invariants, guards and effects, registered per entity

packages/web/src/modules/budgets/
  budget.tsx         its own components, not everyone's
  routes/budgets.tsx screens, loaded on demand
```

The directories are real; the *manifest* is not. What a module still cannot do is declare itself —
its entities live in the one registry, its tables in the one schema, its MCP tools in the adapter's
`tools/` directory, and its routes are wired by name in the shell. That is the shape it would grow
into, and nothing above depends on it:

```
packages/server/src/modules/budgets/
  index.ts        the manifest below
  entities.ts     the registry entries this module contributes
  schema.sql      its tables, concatenated into the schema in dependency order
  invariants.ts   defaults, invariants and guards, registered per entity
  effects.ts      what happens after one of its rows changes
  visibility.ts   canSee* for its rows
  tools.ts        its MCP tools
  routes.ts       endpoints the generic CRUD cannot express
```

```ts
export interface CapabilityModule {
  name: string;
  /** The workspace switch, if it has one. Absent means always on. */
  flag?: keyof WorkspaceFeatures;
  /** Registry entries. Merged into ENTITIES; the client derives its stores from the result. */
  entities: Record<string, EntityDef>;
  /** Write-path hooks, per entity. The dispatcher stays in the kernel. */
  defaults?: Partial<Record<EntityName, DefaultsHook>>;
  invariants?: Partial<Record<EntityName, InvariantHook>>;
  guards?: Partial<Record<EntityName, GuardHook>>;
  /** Reactions to a committed write. Called from the bus, not from writeEntity. */
  effects?: EffectHook[];
  /** "May this person see this row", for the rows this module owns. */
  visibility?: Partial<Record<EntityName, VisibilityRule>>;
  /** MCP tools. Omitted from the tool list when the flag is off. */
  tools?: ToolDef[];
  /** Named reads. The only place this module's SQL lives. */
  queries?: Record<string, QueryFn>;
}
```

Three things this deliberately does **not** do:

- **No dynamic loading, no plugin API, no third-party modules.** The manifests are imported
  statically by one registry file. This is a shape for the code in this repository, not an extension
  point for code outside it — a plugin API is a compatibility promise, and the project does not want
  one yet.
- **No new runtime dependency.** The registry is an array of objects and a `for` loop. That is the
  same trade `architecture.md` makes everywhere else, for the same reason.
- **No change to the sync protocol or the schema.** Every table keeps its name and every entity keeps
  its registry entry; what moves is which *file* the entry is written in. `schema.sql` is
  concatenated from the modules rather than replaced, so an existing database is untouched.

## Getting there

Ordered by ratio of value to risk. Each step ships on its own, keeps the tests green, and is
useful even if the next one never happens. All ten are done.

| # | Step | Touches | Risk | What it buys |
|---|---|---|---|---|
| 1 | ✅ Land `scripts/modules.mjs` and this document | new files | none | The map exists, is generated, and is checked in CI |
| 2 | ✅ Lazy-load capability routes and locale catalogues | `App.tsx`, `lib/i18n.ts`, `main.tsx`, `routes/projects.tsx` | low | First load 1 304 kB → 819 kB, gzipped 385 → 253; warmed in idle so offline is unaffected |
| 3 | ✅ Move the money widget, the chart table, `useSeesMoney` and `ViewConfig` down | 12 files | low | Both client knots gone |
| 4 | ✅ `repo.onWrite()`; `automation` registers at startup | 6 files | low | Kills the last knot with no change to transaction semantics |
| 5 | ✅ Move `storeFile` and `searchWorkspace` down out of `routes/` | 6 files | low | Zero layering exceptions — nothing left to cite as precedent |
| 6 | ✅ Split `lib/mcp.ts` into eleven tool files | 1 file → 13 | medium | The largest file in the repository is now 1 149 lines, and a group is a file |
| 7 | ✅ Hook points in `writeEntity`; the four switchable capabilities moved out | `repo.ts` → 4 modules | medium | The mechanism, proven on rules nothing else depends on |
| 7b | ✅ The core domains, and a `guards` hook for the permission checks | `repo.ts` → 4 modules | medium | `repo.ts` 2 370 → 1 306; `applyInvariants` gone |
| 8 | ✅ `shared/src/modules/work/risk.ts`: one definition of late, for the screen and the assistant | 10 files | low | The report and the chip agree, and the chip is coloured at all — it never was |
| 8b | ✅ Split `routes/auth.ts` along the modules its endpoints belong to | 1 file → 5 | medium | 957 lines → 498; mail, Telegram and push stop living inside identity |
| 9 | ✅ A counter per table on the client, and an observed read set | `lib/store.ts`, `trash.tsx` | low | 103 selector runs per keystroke → 10 |
| 10 | ✅ Every file into `<ring>/<module>/` on all three packages | 220 files | high | The map is read off the tree, not written down — and two rules the old one could not see |

Step 10 was last on purpose, and it earned the place. 220 files moved, no behaviour changed, and
doing it *after* steps 6–9 meant the files being moved were already small and already owned their
rules. Doing it first would have been a reorganisation performed on the same tangle, which is how a
codebase ends up with tidy directories and the same 2 370-line file.

What it bought is not the directory listing. It is that the map above is now *read off the tree*
rather than written down beside it: a list of claims in a script is a second source of truth, and a
file could be moved without moving what claimed it. It also let the layering rule be stated about a
directory instead of a path, which is how it started covering the client.

Two things went wrong, and both are the reason a rename this size is checked rather than trusted:

- **`lib/reactions.ts` and `components/reactions.tsx`** were unambiguous in two directories and not
  in one — `./reactions` resolves `.ts` before `.tsx`, so every importer of the component would have
  silently got the helper. Caught by comparing the resolved import graph before and after, which is
  the only check that looks at what a specifier *means* rather than what it says.
- **`env.ts` computed the workspace root as `resolve(here, '../../..')`** and moved two directories
  deeper. Nothing said so: the typechecker was happy, all 1 340 tests passed, and the server served
  no web build at all. The browser walkthrough was the first thing to notice. The root is found by
  looking for `packages/` now, and `paths.test.ts` pins that and the two other paths the server
  works out for itself at runtime.

### What this does not fix

Honest limits, so nothing here is oversold:

- **`repo.ts` will not get small.** It is 1 282 lines, from 2 370, and most of what is left is the
  merge, the transaction, the tombstones, the search index and the three generic effects. That is a
  thousand lines that genuinely belong together, and no amount of modularity removes them.
- **The kernel stays a single node.** Modules do not change `nextSeq()` or the in-process bus, and
  step 4 deliberately did not either: inverting an import is not the same as making a call
  asynchronous, and only the second one buys a second replica. Every trade-off
  `architecture.md` accepts is still accepted.
- **`i18n` stays the largest kernel module.** 7 599 lines is three catalogues of 2 252 keys. Step 2
  stopped *shipping* the two nobody asked for; it did not make the catalogues smaller, and it should
  not — the compile error on a missing key is worth more than the file size.
- **Modules are not a permission boundary.** A capability is a unit of code, not of trust. Who may
  see what is still `canSeeProject` and the workspace check in front of it, exactly as
  `architecture.md` describes, and moving visibility rules into modules must not make that check
  optional.
- **A write still re-renders every screen on the client.** Step 9 stopped the selectors recomputing,
  which is where the cost was; it did not narrow the subscription, because 89 reads still go
  straight from a render body into the cache and a global re-render is the only thing that keeps
  them fresh. That is a real limit and the fix is not in `store.ts` — it is those 89 reads.

## The rules, and who enforces them

`scripts/modules.mjs` runs in CI. It regenerates the tables above and fails if they have drifted,
and it checks seven rules. Where a rule has an escape hatch it must be *named*, never counted — a
threshold is a budget somebody will spend, and a named exception has to be deleted by the person who
fixes it. Three of the seven carry a list; all three are empty.

1. **Every source file belongs to exactly one module.** Since step 10 the path *is* the claim —
   `packages/<pkg>/src/<ring>/<module>/…` — so belonging to two is impossible and belonging to none
   means sitting outside that shape. Only the shell is exempt, and it is named file by file rather
   than inferred, so an entry point that quietly grows a domain has to argue for itself. This is the
   load-bearing rule: it forces the question "which capability is this?" while somebody still knows
   the answer.
2. **Packages point one way.** `shared` imports neither of the others, `web` never imports `server`,
   `server` never imports `web`, and `@kolibri/shared` is the one bare specifier that may cross.
3. **Layers point one way.** A module's `routes/` is its top: only the shell, which composes
   everything, may import one. It used to read "`server/src/lib` may not import `server/src/routes`",
   which was the same idea in the only two directories that then existed; expressed as a directory
   it now covers the client too, and immediately found four violations there and in the MCP adapter
   that the old shape could not see. None of them were new — only the rule was — and all four came
   apart the same way the ones before them did, by moving the shared thing down: `useUnreadMessages`
   into `chat/unread.ts`, `resourceUrl` into `oauth/resource.ts`, and `automation.tsx`, which turned
   out not to be a route at all. **No exceptions** — `KNOWN_LAYERING` is empty again.
4. **No import knots.** Reported as the whole set of files that can reach each other rather than as
   one lap around it, so the list does not reshuffle when an unrelated file is renamed. **No
   exceptions** — `KNOWN_KNOTS` is empty too. All three came apart the same way, and none by
   rearranging the callers: the shared thing moved to where everyone could reach it.
5. **The rings point one way.** A capability may lean on the kernel, an adapter on both, and
   nothing leans outward. A kernel that leans back on a capability means neither can be understood,
   tested or replaced without the other; a capability reaching for an adapter has chosen a provider
   on everybody's behalf. The way out of both is the same and the repository now does it nine
   times: the inner ring says what it needs and the outer one registers. Which module asks for what,
   and who supplies it, is [generated above](#what-each-ring-asks-the-next-one-for) from a `@port`
   tag on the declaring function — that sentence used to carry the list, and the list was wrong.
   Two things do not count, and both are argued above: a `routes/` file composes, and an
   `import type` is erased. **No exceptions** — `KNOWN_OUTWARD` is empty. The last five came apart
   the way every other one did, by moving the shared thing to the ring it belonged in rather than
   arguing for it.
6. **Module dependencies are acyclic.** Modules are not independent and pretending otherwise would
   be a lie about the domain. What they can be is *ordered*, and they are — every ring, no cycles.
   This is rule 4 one floor up and is not implied by it: two modules can each import the other
   without any single file doing so. It covers the kernel and the adapters as well as the
   capabilities, because emptying `KNOWN_OUTWARD` is what showed why it should. **No exceptions**,
   and none needed.
7. **Every port is filled, from further out.** A port is how a module gets what rule 5 forbids it to
   reach for, so a port nobody fills is a module asking for something it will never get, and a port
   filled from its own ring is ceremony — a direct import would have been legal there, and the
   indirection bought nothing. The tag is checked too: `@port` on anything but an exported function
   would drop a row from the table above and three figures with it, silently, which is the failure
   the tag was added to stop. **No exceptions**, and no list for them — a same-ring port to break a
   rule-6 cycle would be a real one, and whoever needs the first writes it down then.

There is an illustrated version of all this in [`module-map.html`](module-map.html) beside this
file. It is not a second source: its tally, its legend, its inventory and three of its tables are
written into it by the same script that writes the tables here, and the numbers in its sentences are
claimed in `figures.mjs` like every other figure. It was hand-maintained until it moved into the
tree, which is why it had drifted to `227 files` and to five ring exceptions that no longer existed.

The checks have a suite of their own, `scripts/checks.test.mjs`. Every rule above was proved once by
breaking the tree by hand and watching the check complain, and then that proof was thrown away; the
suite is those proofs kept. Each case copies the tree, breaks one thing in the copy, runs the checker
against it and asserts what it says — and two of the cases break the *script* rather than the tree,
because a walk that quietly returns nothing is the regression that would leave every check passing
and every table wrong.

```bash
npm run check:modules      # the seven rules and the tables; exit 1 on a violation
npm run modules            # the inventory on the terminal
npm run modules -- --fix   # rewrite the tables above
npm run modules -- --json  # the same rows, for anything that draws them
npm run modules -- --graph # the graph as numbers, for figures.mjs
```

A fifth rule is the typechecker's rather than this script's: **`noUnusedLocals`**, on since the
route split. Every move above left names imported into files that no longer used them — 68 of them
by the end, most from steps 6 and 7b, which is what an unfinished move looks like from the outside.
A name imported and not used is dead weight a reader has to rule out, and there is no reason to
check it by hand when `tsc` will. `noUnusedParameters` is deliberately left off: an unused argument
is usually the shape of a callback rather than a mistake.

### The direction rules, and which of them are rules now

The rules about *direction* rather than shape became checkable when step 10 put the modules in
directories. The first measurement said 108 crossings, which was two counting mistakes on top of a
real problem:

- **A `routes/` file composes.** That is what a route is, wherever it sits. Counting a route's
  imports as ring dependencies made the kernel look like it depended on nine capabilities when what
  it had was nine endpoints. 108 → 44.
- **An `import type` is erased.** A kernel file naming a capability's *type* couples the build and
  not the run: delete the capability and the typecheck fails, nothing else. Worth knowing, worth
  counting separately. 44 → 35 that survive compilation, and 5 that do not.

What was left was worth doing, and all of it is done:

| | before | now |
|---|---|---|
| kernel → a capability | 5 | **0** |
| kernel → an adapter | 3 | **0** |
| capability → an adapter | 12 | **0** |
| capability → another capability | 14 | 17 |
| adapter → a capability | 1 | 16 |

The last row is the one that went up, and on purpose: an adapter is *supposed* to lean on a
capability. It does so sixteen times now precisely because the outward edges were turned round —
every inversion below leaves the adapter holding the reference that used to point the wrong way.

**Rule 5 was the kernel's half first, and the build enforces it.** Four inversions and a split got
there. There are **11** ports now, across **9** modules, and **11** of them are filled — every one a
call from an outer ring into an inner one, which is the whole trick. The table above is generated
from the `@port` tag on each declaring function, so a port that grows, moves or stops being filled
shows up there rather than in somebody's memory of this paragraph:

- **`repo.ts` called `notify` and `fireWebhooks` by name**, so the write path held the notification
  copy and the webhook payload shapes. It offers `onCommitted` now — the counterpart of `onWrite`,
  for effects that leave the process and cannot be rolled back — and both register. `repo.ts` went
  from 1 281 lines to 855, and ten of the sixteen `if (entity === …)` branches finding 2 left behind
  went with them.
- **`storage.ts` imported the S3 client** and branched on `kind === 's3'` in six functions. It
  offers a `Backend` interface; the disk is its own default and S3 registers.
- **`sync.ts` called into `chat/presence`** four times. It offers `onStream` and presence registers,
  which gave the client the `wiring.ts` the server already had.
- **`ui.tsx` built a link into the guide.** `Empty` asks for a hint; the guide says what one looks
  like. A build without the guide shows none instead of failing to compile.
- **`AppShell.tsx` was not design system.** It composes work, chat and the guide — that is a shell.
  The frame moved to `src/AppShell.tsx`; what every route actually wanted from it, `Header` and the
  theme, stayed in `kernel/design-system/chrome.tsx`.

**The three that were left were a format filed as a provider.** They were argued for rather than
fixed: `env.ts` and `settings.ts` are the one place configuration is parsed — which is what lets
`check:compose` prove every documented variable is reachable — and parsing `KOLIBRI_SMTP_URL` means
knowing what an SMTP URL looks like, so the kernel imported the SMTP client to ask. The argument was
sound and the conclusion was wrong: what it needed was not the client but the *shape*, and a shape
is not a provider. `kernel/mail` holds the two — what an email address is, and how a URL spells a
relay — and `adapters/mail` keeps the sockets, the RFC 5321 conversation and the retries.

The address half had a second reader the rule could not see. `isEmailAddress` was also imported by
`identity/routes/auth.ts` and `identity/routes/workspaces.ts` — signing up and inviting somebody —
and rule 3 excused both because a `routes/` file composes. Four things ask what an address is and
exactly one of them is sending mail; an account is named by an address whether or not this instance
can post to one. So the move was not a technicality to satisfy a checker: three of the four callers
had no business knowing where the SMTP client lives.

**The capability ring turned out to be two questions, not one.**

*Reaching outward* — a capability choosing an adapter — is the same fault as the kernel's and came
apart the same way. Twelve became none:

- **`notify` called push and Telegram by name.** A notification is a row plus every channel that
  carries it, and *which* channels exist is not the notification's business. It offers a delivery;
  each adapter registers one.
- **`review.ts` imported all three model providers** and switched on the configured name, so adding
  a fourth meant editing a capability. `modules/ai-review/model.ts` is the port — the shape of the
  question, the shape of a failure, and a place to say "I can answer" — and `adapters/ai` fills it.
- **The scheduler ran two adapters' housekeeping by name.** It offers a chore; Telegram and webhooks
  register theirs.
- **`backups.ts` used the S3 client directly** to copy a snapshot offsite. It writes to the store
  the instance is already configured with, under a prefix of its own — which store that is happens
  to be the one question it has no reason to answer. It goes through `storage` now.
- **`zip.ts` was never an adapter.** It imports `node:zlib` and nothing else; a container format is
  a format, and it sits beside `mime.ts` in `kernel/files` where planning and transfer are equal
  callers.
- **The share dialog was never an adapter.** The old note had the diagnosis and stopped there:
  `share` is an adapter on the server, where it renders a board for somebody holding a link, and a
  small piece of product UI on the client, where it *makes* the link. The second half is a screen —
  it imports the design system, the store and the session, and nothing outside the process — so it
  is a capability, and `modules/share` is where it lives. Intake, saved views and the page tree lean
  on it the way capabilities are allowed to lean on each other.

One noun now names a module in two rings, twice: `mail` and `share` each have a kernel or capability
half and an adapter half. That is the ring boundary drawn through a domain rather than around it,
and it is the clearest thing in the inventory — so `ABOUT` is keyed by `ring/name` where it happens,
because one sentence covering both halves would have to be vague enough to be wrong about each.

*Depending on each other* is a different question and the answer is not zero. `work` imports
`planning` because the timeline view of a task list is a Gantt chart; no inversion changes that, and
a registry invented for each one-off relationship would be the counted budget this document argues
against, in a costume. Seventeen imports across fifteen module pairs, and **no cycles** — so any
capability can be read knowing only what it leans on, and one that nothing leans on can be deleted
without touching the rest. Rule 6 keeps it that way, and it is not implied by rule 4: two modules can
each import the other without any single *file* doing so, which is a knot the file-level check walks
straight past.

### Is there a fifth ring? No.

The table above raises it: capabilities are plainly not peers, so perhaps the ones others lean on are
a ring of their own, between the kernel and the rest. Three things say no, and the last one is
decisive.

**There is no stratum.** A ring is not "the important ones", it is a *set that depends on nothing
above it, and that the things above it depend on*. There are **17** capabilities and **15** edges
between them. The most leaned-on is `pages`, and **3** of them lean on it. **6** — `automation`,
`chat`, `infrastructure`, `intake`, `kpis`, `mail` — are leaned on by nobody at all, and three of those
lean on nothing either. Of the **7 214** ways to split this graph into a lower part that depends on nothing
above it and an upper part, the lower part that the most capabilities lean on is `budgets`, `pages`
and `share`: **3** modules that only **5** of the rest lean on. Every split was tried, so that is the
strongest case for a fifth ring available here — and it would be three modules that most of the ring
above them never mentions.

**The candidate disqualifies itself.** `work` looks like the foundation — it is the biggest domain
and other capabilities lean on it — and it leans on five of them: `ai-review`, `pages`, `planning`,
`share` and `time`. Put `work` in a ring below capabilities and all five of those imports point
outward, which is what rule 5 forbids: a ring that breaks the ring rule on the day it is drawn is
not a ring. Move the five down with it and the split is legal again — but it is then one of the
7 214 counted above, and not the best of them.

**A ring has to buy a rule.** Each of the four exists because it makes something checkable that
otherwise is not: what is always present, what a switch can remove, what faces outside, what merely
composes. A fifth would have to make "capabilities are ordered" checkable — and that is already rule
6, as acyclicity, which needs no ring to say it and now says it of every ring.

> **A correction.** An earlier version of this section put `work` at "level 2 with seven capabilities
> under it" and read that as `work` being closer to the kernel. The number was a longest-dependency-
> chain depth, which measures what a module *stands on*, not what stands on *it* — precisely
> backwards for the question being asked. Counted the right way round, the most depended-upon
> capability is `pages`, which leans on nothing. The table above is generated from the graph now, so
> the next person to ask gets the measurement rather than the memory.

**What changes instead is the description.** This document has called capabilities peers. They are
not: they are a sparse partial order, generated above — 17 members, 15 edges between them, no
cycles. That is a smaller and truer claim than a fifth ring, and it costs nothing to keep
honest.
